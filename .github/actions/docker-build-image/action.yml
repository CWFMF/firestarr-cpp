# requires docker setup elsewhere
name: docker-build-image
description: 'Build docker image for specific platform'

inputs:
  target:
    required: true
    type: string
  package-name:
    required: true
    type: string
  platform:
    required: true
    type: string
  registry:
    required: true
    type: string
  image-missing:
    required: true
    type: boolean

env:
  BUILDX_NO_DEFAULT_ATTESTATIONS: 1

outputs:
  package:
    description: "Container package"
    value: ${{ steps.output.outputs.package }}

runs:
  using: "composite"
  steps:
    - name: Prepare environment
      shell: bash
      run: |
        registry=${{ inputs.registry }}
        repo=${registry}/${GITHUB_REPOSITORY@L}
        package=${repo}/${{ inputs.package-name }}
        registry=${repo}/cache
        cached=${registry}:${{ github.sha }}${{ inputs.suffix }}
        echo "PACKAGE=${package}" >> $GITHUB_ENV
        echo "REGISTRY=${registry}" >> $GITHUB_ENV
        echo "CACHED=${cached}" >> $GITHUB_ENV
        platform=${{ inputs.platform }}
        ref_name=${{ github.ref_name }}
        echo "PLATFORM_PAIR=${platform//\//}" >> $GITHUB_ENV
        echo "CACHE_REF=${ref_name//\//}" >> $GITHUB_ENV

    - name: ${{ inputs.target }} - Docker meta
      if: ${{ inputs.image-missing }}
      id: meta
      uses: docker/metadata-action@v6
      with:
        images: ${{ env.PACKAGE }}

    - name: ${{ inputs.target }} - Build and push by digest
      if: ${{ inputs.image-missing }}
      id: build
      uses: docker/build-push-action@v7
      with:
        context: .
        file: .docker/Dockerfile
        target: ${{ inputs.target }}
        platforms: linux/${{ inputs.platform }}
        labels: ${{ steps.meta.outputs.labels }}
        tags: ${{ env.PACKAGE }}
        push: ${{ github.event_name != 'pull_request' }}
        outputs: type=image,push-by-digest=true,name-canonical=true,push=true
        cache-from: |
          type=registry,ref=${{ env.REGISTRY }}:${{ env.PLATFORM_PAIR }}-${{ github.sha }}
          type=registry,ref=${{ env.REGISTRY }}:${{ env.PLATFORM_PAIR }}-${{ env.CACHE_REF }}
          type=registry,ref=${{ env.REGISTRY }}:${{ env.PLATFORM_PAIR }}
          type=registry,ref=${{ env.REGISTRY }}
        cache-to: |
          type=registry,mode=max,ref=${{ env.REGISTRY }}:${{ env.PLATFORM_PAIR }}-${{ env.CACHE_REF }}
          type=registry,mode=max,ref=${{ env.REGISTRY }}:${{ env.PLATFORM_PAIR }}-${{ github.sha }}
          type=registry,mode=max,ref=${{ env.REGISTRY }}:${{ env.PLATFORM_PAIR }}

    - name: ${{ inputs.target }} - Export digest
      if: ${{ inputs.image-missing }}
      shell: bash
      run: |
        mkdir -p ${{ runner.temp }}/digests-${{ inputs.target }}
        digest="${{ steps.build.outputs.digest }}"
        touch "${{ runner.temp }}/digests-${{ inputs.target }}/${digest#sha256:}"

    - name: ${{ inputs.target }} - Upload digest
      if: ${{ inputs.image-missing }}
      uses: actions/upload-artifact@v7
      with:
        name: digests-${{ inputs.target }}-${{ env.PLATFORM_PAIR }}
        path: ${{ runner.temp }}/digests-${{ inputs.target }}/*
        if-no-files-found: error
        retention-days: 1

    - name: Set outputs
      id: output
      shell: bash
      run: |
        echo "package=${{ env.PACKAGE }}" >> $GITHUB_OUTPUT
